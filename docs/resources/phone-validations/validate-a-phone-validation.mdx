---
id: validate-a-phone-validation
title: Validate a Phone Validation
slug: /resources/validate-a-phone-validation
description: Verify a phone OTP with phone, countryCode, and otp
keywords:
  - v2/phone-validations
  - /v2/phone-validations
  - PUT v2/phone-validations
  - PUT /v2/phone-validations
  - phone-validations validate
  - api.verifik.co/v2/phone-validations
  - 'https://api.verifik.co/v2/phone-validations'
  - otp verify phone
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

### Endpoint

```
PUT https://api.verifik.co/v2/phone-validations
```

Confirms the OTP the user received. Verifik looks up the latest **sent** (and not expired) Phone Validation for that phone — including **manual / Smart Tools** records and project-flow records.

:::tip Pair with send
Standalone OTPs are created with [`POST /v2/phone-validations/manual`](/resources/create-a-manual-phone-validation). App registration OTPs use [`POST /v2/phone-validations/app-registration`](/resources/phone-validations/create-an-app-registration-phone-validation).
:::

### Headers

| Name | Value |
| --- | --- |
| Content-Type | `application/json` |
| Authorization | `Bearer {YOUR_ACCESS_TOKEN}` |

### Body parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `phone` | string | Yes | Same national number used when sending the OTP. |
| `countryCode` | string | Yes | Dial code starting with `+`. |
| `otp` | number | Yes | Numeric code the user received. |
| `phoneGateway` | string | No | `sms` or `whatsapp` — narrows the lookup when both channels were used. |
| `project` | string | No | Optional project filter for flow-based records. |
| `projectFlow` | string | No | Optional project-flow filter (preferred when available). |

### Request examples

<Tabs groupId="language">
<TabItem value="curl" label="cURL">

```bash
curl -X PUT "https://api.verifik.co/v2/phone-validations" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "3001234567",
    "countryCode": "+57",
    "otp": 123456,
    "phoneGateway": "whatsapp"
  }'
```

</TabItem>
<TabItem value="nodejs" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.put(
  "https://api.verifik.co/v2/phone-validations",
  {
    phone: "3001234567",
    countryCode: "+57",
    otp: 123456,
    phoneGateway: "whatsapp",
  },
  {
    headers: {
      Authorization: "Bearer YOUR_ACCESS_TOKEN",
      "Content-Type": "application/json",
    },
  }
);

console.log(data);
```

</TabItem>
<TabItem value="python" label="Python">

```python
import requests

response = requests.put(
    "https://api.verifik.co/v2/phone-validations",
    headers={
        "Authorization": "Bearer YOUR_ACCESS_TOKEN",
        "Content-Type": "application/json",
    },
    json={
        "phone": "3001234567",
        "countryCode": "+57",
        "otp": 123456,
        "phoneGateway": "whatsapp",
    },
)

print(response.json())
```

</TabItem>
<TabItem value="php" label="PHP">

```php
<?php

$client = new \GuzzleHttp\Client();

$response = $client->request('PUT', 'https://api.verifik.co/v2/phone-validations', [
  'headers' => [
    'Authorization' => 'Bearer YOUR_ACCESS_TOKEN',
    'Content-Type' => 'application/json',
  ],
  'json' => [
    'phone' => '3001234567',
    'countryCode' => '+57',
    'otp' => 123456,
    'phoneGateway' => 'whatsapp',
  ],
]);

echo $response->getBody();
```

</TabItem>
</Tabs>

### Success response

```json
{
  "data": {
    "_id": "66f0a1b2c3d4e5f678901234",
    "status": "validated",
    "countryCode": "+57",
    "phone": "3001234567",
    "phoneGateway": "whatsapp",
    "source": "manual",
    "validatedAt": "2026-07-29T22:40:00.000Z"
  }
}
```

### Common errors

| Status | Message | When |
| --- | --- | --- |
| 403 | `otp_does_not_match` | Code does not match the pending OTP. |
| 412 | `phoneValidation_has_expired` | No non-expired `sent` record found, or the session expired. |
| 409 | `MissingParameter` | Required body fields missing. |

### Related

- [Create a Manual Phone Validation](/resources/create-a-manual-phone-validation)  
- [Phone Validations overview](/resources/phone-validations)  
