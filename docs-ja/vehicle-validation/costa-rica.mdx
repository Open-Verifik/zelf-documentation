---
id: costa-rica
title: 🇨🇷 コスタリカ — 車両情報
description: GET /v2/cr/vehicle で必要なナンバープレートを指定し、コスタリカの車両を照会します。利用可能な場合、所有者、仕様、違反、所有者履歴を返します。
slug: /vehicle-validation/costa-rica
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "コスタリカのナンバープレート別車両検証 | Verifik API",
	description:
		"ナンバープレートでコスタリカの車両を照会します。GET /v2/cr/vehicle に plate パラメーターを指定し、所有者、技術データ、履歴を取得します。",
	articleSection: "API ドキュメント",
	keywords: "コスタリカ 車両, ナンバープレート, v2/cr/vehicle, フリート, 保険",
	about: {
		"@type": "Thing",
		name: "車両検証 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 🇨🇷 コスタリカ — 車両情報

本サービスは、**ナンバープレート** を用いてコスタリカで登録された車両に関する詳細情報を返します。一般的なデータには、現在の所有者、車両仕様（排気量、評価額、重量、定員、出力、駆動方式）、**違反** 履歴、ソースが公開している場合は **過去の所有者** が含まれます。

フリート確認、保険、コンプライアンスのワークフローに使用します。

## API リファレンス

### エンドポイント

```
https://api.verifik.co/v2/cr/vehicle
```

クエリパラメーター **`plate`**（スペースや句読点なし）を含む GET リクエストを送信します。レスポンスには、構造化された車両データ、所有者フィールド、利用可能な場合は履歴配列が含まれます。

### ヘッダー

| 名前          | 値                 |
| ------------- | ------------------ |
| Accept        | `application/json` |
| Authorization | `Bearer <token>`   |

### パラメーター

| 名前 | 型 | 必須 | 説明 | 例 |
| ---- | ---- | ---- | ---- | ---- |
| `plate` | string | **はい** | 照会するナンバープレート。スペースやドットを含めずに入力してください。 | `AA123BA` |

### リクエスト

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/cr/vehicle", {
  params: { plate: "AA123BA" },
  headers: { Accept: "application/json", Authorization: `Bearer ${process.env.VERIFIK_TOKEN}` },
});
console.log(data);
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import http.client

conn = http.client.HTTPSConnection("api.verifik.co")
payload = ''
headers = {}
conn.request("GET", "/v2/cr/vehicle?plate=AA123BA", payload, headers)
res = conn.getresponse()
data = res.read()
print(data.decode("utf-8"))
```

  </TabItem>
  <TabItem value="php" label="PHP">

```php
<?php
require_once 'HTTP/Request2.php';
$request = new HTTP_Request2();
$request->setUrl('https://api.verifik.co/v2/cr/vehicle?plate=AA123BA');
$request->setMethod(HTTP_Request2::METHOD_GET);
$request->setConfig(array(
  'follow_redirects' => TRUE
));
try {
  $response = $request->send();
  if ($response->getStatus() == 200) {
    echo $response->getBody();
  }
  else {
    echo 'Unexpected HTTP status: ' . $response->getStatus() . ' ' .
    $response->getReasonPhrase();
  }
}
catch(HTTP_Request2_Exception $e) {
  echo 'Error: ' . $e->getMessage();
}
?>
```

  </TabItem>
  <TabItem value="swift" label="Swift">

```swift
var request = URLRequest(url: URL(string: "https://api.verifik.co/v2/cr/vehicle?plate=AA123BA")!,timeoutInterval: Double.infinity)
request.httpMethod = "GET"

let task = URLSession.shared.dataTask(with: request) { data, response, error in 
  guard let data = data else {
    print(String(describing: error))
    return
  }
  print(String(data: data, encoding: .utf8)!)
}

task.resume()
```

  </TabItem>
</Tabs>

### レスポンス

<Tabs>
  <TabItem value="200" label="200">

```json
{
  "data": {
    "currentOwner": "MARIA ELENA LOPEZ GARCIA",
    "dataVehicle": {
      "engineDisplacement": "1600",
      "estateValue": "$15000000",
      "grossWeight": "1850 kg",
      "netWeight": "1420 kg",
      "noVin": "JTD00000001",
      "occupants": "5",
      "power": "120 HP",
      "traction": "4x2"
    },
    "infractions": [],
    "ownerHistory": [
      {
        "date": "15/01/2020",
        "fullName": "MARIA ELENA LOPEZ GARCIA"
      },
      {
        "date": "10/06/2015",
        "fullName": "JOSE ANTONIO PEREZ RODRIGUEZ"
      }
    ],
    "plate": "AA123BA",
    "vehicle": "TOYOTA COROLLA XLI"
  },
  "signature": {
    "dateTime": "July 27, 2026 3:00 PM",
    "message": "Certified by Verifik.co"
  }
}
```

  </TabItem>
  <TabItem value="404" label="404">

```json
{
    "code": "NotFound",
    "message": "Record not found."
}
```

  </TabItem>
  <TabItem value="409" label="409">

```json
{
  "code": "MissingParameter",
  "message": "missing plate\n"
}
```

  </TabItem>
</Tabs>

## 機能

- **車両情報の取得**: コスタリカの車両登記簿から包括的な車両詳細を取得します。
- **ナンバープレート検証**: コスタリカで登録された車両のナンバープレートを検証します。
- **現在の所有者情報**: 車両の現在の所有者情報を取得します。
- **車両仕様**: 排気量、評価額、重量、定員、出力、駆動方式を取得します。
- **違反履歴**: 交通違反の完全な履歴にアクセスできます。
- **所有者履歴**: 過去の車両所有者の時系列リストを参照できます。
- **司法当局**: 違反を扱う司法当局の情報を取得します。
- **チケット情報**: 違反のチケット番号やサマリー番号にアクセスできます。
- **複数のプログラミング言語に対応**: JavaScript、Python、PHP、Swift をサポートします。
- **リアルタイムデータ**: 最新の車両情報にアクセスできます。
- **包括的なエラーハンドリング**: 各種シナリオに対応した詳細なエラーレスポンスを提供します。
- **コスタリカ登録機関との連携**: コスタリカの公式車両データベースに直接アクセスできます。
