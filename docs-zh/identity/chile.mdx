---
id: chile
title: 智利公民
description: "核验智利国民身份证（RUN/RUT）：通过对接民事登记官方数据源核实姓名与身份字段，适用于 KYC、客户开通与反欺诈场景。"
slug: /identity/chile
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "智利身份核验（RUN）| Verifik KYC API",
	description:
		"核验智利国民身份证（RUN/RUT）：通过对接民事登记官方数据源核实姓名与身份字段，适用于 KYC、客户开通与反欺诈场景。",
	articleSection: "API 文档",
	keywords: "身份核验, KYC API, 智利 RUN 核验, 官方数据源, 反欺诈",
	about: {
		"@type": "Thing",
		name: "身份核验 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 智利身份核验

Verifik 的身份核验 API 可使用智利的国民唯一编号（RUN）对智利公民进行身份核实。该接口旨在简化您的 KYC（了解您的客户）流程、防范欺诈，并轻松满足合规要求。

我们专为需要快速、安全且自动化确认用户、员工或客户真实身份的企业打造此集成。

## 此 API 验证哪些内容？

我们的 API 直接对接官方记录，验证以下内容：

-   **姓名全称与 RUN**：支持 *Rol Único Nacional*（RUN）。
-   **身份匹配**：确认所提供姓名与 RUN 号码相符。

通过验证这些信息，您可以确信对接的人员真实存在并持有有效证件，从而显著降低身份冒用与欺诈风险。

## API 参考

### Endpoint

```
https://api.verifik.co/v2/cl/cedula
```

核验智利国民身份证（RUN/RUT）：通过对接民事登记官方数据源核实姓名与身份字段，适用于 KYC、客户开通与反欺诈场景。

### 请求头（Headers）

| 名称 | 取值 |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### 参数（Parameters）

| 名称 | 类型 | 必填 | 描述 |
| --- | --- | --- | --- |
| `documentType` | string | 是 | 允许取值：`RUN`。 |
| `documentNumber` | string | 是 | RUN 数值，不含空格或分隔符。 |

### 请求（Request）

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/cl/cedula", {
  params: { documentType: "RUN", documentNumber: "212957739" },
  headers: { Accept: "application/json", Authorization: `Bearer ${process.env.VERIFIK_TOKEN}` },
});
console.log(data);
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import os, requests

url = "https://api.verifik.co/v2/cl/cedula"
headers = {"Accept": "application/json", "Authorization": f"Bearer {os.getenv('VERIFIK_TOKEN')}"}
params = {"documentType": "RUN", "documentNumber": "212957739"}
r = requests.get(url, headers=headers, params=params)
print(r.json())
```

  </TabItem>
</Tabs>

### 响应（Response）

<Tabs>
  <TabItem value="200" label="200">

```json
{
  "data": {
    "documentType": "RUN",
    "documentNumber": "212957739",
    "firstName": "Carlos",
    "lastName": "Rodríguez",
    "fullName": "Carlos Rodríguez"
  },
  "signature": {"message": "Certified by Verifik.co", "dateTime": "January 16, 2024 3:44 PM"},
  "id": "CL001"
}
```

  </TabItem>
  <TabItem value="400" label="400">

```json
{
  "message": "Invalid RUN number",
  "code": "INVALID_RUN"
}
```

  </TabItem>
</Tabs>

### 备注

-   提交 RUN 时请勿包含空格或分隔符。

---

## 常见使用场景

-   **金融科技与银行**：在开户或贷款申请过程中即时核验身份。
-   **电商与配送**：在用户与配送员入驻您的平台前完成身份核验。
-   **人力资源与招聘**：将候选人证件核验纳入招聘流程。
-   **保险与医疗**：在出单或提供医疗福利前确认身份。

## 官方数据源与可靠性

我们直接对接智利政府官方数据源（Registro Civil），确保您获得经过验证且最新的信息。
每次查询均严格遵循安全与监管标准。

## 主要优势

-   **自动化合规**：在不增加用户摩擦的前提下简化 KYC 检查并防范欺诈。
-   **即时结果**：在数秒内完成核验，适用于实时数字化开通。
-   **可信数据**：采用直接来源于官方记录的数据。
-   **轻松集成**：通过 REST API 或兼容 SDK 即可便捷接入。

## 合规与安全

我们高度重视您的数据安全。Verifik 采用高级加密（HTTPS / TLS 1.3）和严格的隐私管理标准以确保机密性。
我们的服务全天候 24/7 监控可用性，并提供基于角色的访问控制以保障团队访问安全。

## 关于 Verifik

Verifik 是拉丁美洲领先的身份核验、合规与反欺诈平台。
我们的 API 自动化 KYC、KYB、AML 与生物识别验证流程，将企业与哥伦比亚、墨西哥、秘鲁、智利、乌拉圭、阿根廷及更多地区的官方数据源相连。
