---
id: costa-rica
title: 哥斯达黎加公民（CCCR）
description: 通过 GET /v2/cr/cedula 与 documentType、documentNumber 在哥斯达黎加进行 CCCR 国民身份证核验，适用于 KYC 与反欺诈。
slug: /identity/costa-rica
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "哥斯达黎加国民身份证（CCCR）核验 | Verifik KYC API",
	description:
		"核验哥斯达黎加国民身份证（CCCR）：通过 GET /v2/cr/cedula 提交 documentType CCCR 与 documentNumber 即可获得结构化身份数据。",
	articleSection: "API 文档",
	keywords: "身份核验, KYC API, 哥斯达黎加 CCCR 核验, 官方数据源, 反欺诈",
	about: {
		"@type": "Thing",
		name: "身份核验 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 哥斯达黎加身份核验

Verifik 的身份核验 API 可基于政府官方数据对哥斯达黎加公民进行身份核实。该接口旨在简化您的 KYC（了解您的客户）流程、防范欺诈，并轻松满足合规要求。

我们专为需要快速、安全且自动化确认用户、员工或客户真实身份的企业打造此集成。

## 此 API 验证哪些内容？

我们的 API 直接对接官方记录，验证以下内容：

-   **姓名全称与 CCCR 号码**：支持 *Cédula de Identidad Costarricense*（CCCR）。
-   **身份匹配**：确认所提供姓名与 CCCR 号码相符。

通过验证这些信息，您可以确信对接的人员真实存在并持有有效证件，从而显著降低身份冒用与欺诈风险。

## API 参考

### Endpoint

```
https://api.verifik.co/v2/cr/cedula
```

该接口基于官方数据源核验 **CCCR**（*Cédula de Identidad Costarricense*）。请通过 GET 请求传入 **`documentType`** 为 `CCCR` 与 **`documentNumber`**（不含空格的 cédula 号码）；响应包含结构化的姓名字段，适用于 **KYC**、客户开通与反欺诈核查。

### 请求头（Headers）

| 名称           | 取值                |
| -------------- | ------------------- |
| Accept         | `application/json`  |
| Authorization  | `Bearer <token>`    |

### 参数（Parameters）

| 名称              | 类型    | 必填 | 描述                            |
| ----------------- | ------- | ---- | ------------------------------- |
| `documentType`    | string  | 是   | 证件类型。允许取值：`CCCR`。     |
| `documentNumber`  | string  | 是   | 不含空格的 cédula 号码。         |

沙盒客户端可以使用文档编号 `010000001`–`010000010`（规范形式 `10000001`–`10000010`）获取稳定的演示资料；`090040401` 返回404；`090040402` 返回下方所示的"需要更新"响应。

### 请求（Request）

<Tabs>
  <TabItem value="node" label="Node.js">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/cr/cedula", {
  params: { documentType: "CCCR", documentNumber: "010000001" },
  headers: { Accept: "application/json", Authorization: `Bearer ${process.env.VERIFIK_TOKEN}` },
});
console.log(data);
```

  </TabItem>
  <TabItem value="php" label="PHP">

```php
<?php
$ch = curl_init("https://api.verifik.co/v2/cr/cedula");
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    "Accept: application/json",
    "Authorization: Bearer " . getenv("VERIFIK_TOKEN")
]);
$query = http_build_query([
    "documentType" => "CCCR",
    "documentNumber" => "010000001"
]);
curl_setopt($ch, CURLOPT_URL, "https://api.verifik.co/v2/cr/cedula?".$query);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
$response = curl_exec($ch);
curl_close($ch);
echo $response;
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import os, requests

url = "https://api.verifik.co/v2/cr/cedula"
headers = {"Accept": "application/json", "Authorization": f"Bearer {os.getenv('VERIFIK_TOKEN')}"}
params = {"documentType": "CCCR", "documentNumber": "010000001"}
r = requests.get(url, headers=headers, params=params)
print(r.json())
```

  </TabItem>
  <TabItem value="go" label="Go">

```go
package main

import (
    "fmt"
    "net/http"
)

func main() {
    url := "https://api.verifik.co/v2/cr/cedula?documentType=CCCR&documentNumber=010000001"
    req, _ := http.NewRequest("GET", url, nil)
    req.Header.Set("Accept", "application/json")
    req.Header.Set("Authorization", "Bearer "+getenv("VERIFIK_TOKEN"))
    resp, _ := http.DefaultClient.Do(req)
    defer resp.Body.Close()
    fmt.Println(resp.Status)
}
```

  </TabItem>
 </Tabs>

### 响应（Response）

<Tabs>
  <TabItem value="200" label="200">

```json
{
  "data": {
    "documentType": "CCCR",
    "documentNumber": "010000001",
    "firstName": "MARIA ELENA",
    "lastName": "LOPEZ GARCIA",
    "fullName": "MARIA ELENA LOPEZ GARCIA"
  },
  "signature": {"message": "Certified by Verifik.co", "dateTime": "July 27, 2026 3:00 PM"}
}
```

  </TabItem>
  <TabItem value="200-renewal" label="200 (需要更新)">

```json
{
  "data": {
    "documentType": "CCCR",
    "documentNumber": "020000002",
    "status": "requires_renewal",
    "expired": true,
    "message": {
      "es": "La fecha de vencimiento de su última solicitud, es menor a doce meses, por lo que debe renovar su documento en las oficinas del TSE",
      "en": "The expiration date on file for this citizen's last request is less than twelve months away, so the document must be renewed in person at a TSE office."
    }
  },
  "signature": {"message": "Certified by Verifik.co", "dateTime": "July 27, 2026 3:00 PM"}
}
```

  </TabItem>
  <TabItem value="400" label="400">

```json
{
  "message": "Invalid document number",
  "code": "INVALID_DOCUMENT"
}
```

  </TabItem>
  <TabItem value="401" label="401">

```json
{
  "message": "Authentication required",
  "code": "UNAUTHORIZED"
}
```

  </TabItem>
  <TabItem value="500" label="500">

```json
{
    "code": "InternalServerError",
    "message": "Server error."
}
```

  </TabItem>
 </Tabs>

### 备注

- 提交编号时请勿包含空格或圆点。
- 根据TSE的更新规则不符合在线补办资格的身份证（cédula）仍会成功返回（`200`），包含 `status: "requires_renewal"` 和 `expired: true`，而不是返回错误。双语（`es`/`en`）的 `message` 对象说明该公民必须亲自前往TSE办公室办理证件更新。

---

## 常见使用场景

-   **金融科技与银行**：在开户或贷款申请过程中即时核验身份。
-   **电商与配送**：在用户与配送员入驻您的平台前完成身份核验。
-   **人力资源与招聘**：将候选人证件核验纳入招聘流程。
-   **保险与医疗**：在出单或提供医疗福利前确认身份。

## 官方数据源与可靠性

我们直接对接哥斯达黎加政府官方数据源，确保您获得经过验证且最新的信息。
每次查询均严格遵循安全与监管标准。

## 主要优势

-   **自动化合规**：在不增加用户摩擦的前提下简化 KYC 检查并防范欺诈。
-   **即时结果**：在数秒内完成核验，适用于实时数字化开通。
-   **可信数据**：采用直接来源于官方记录的数据。
-   **轻松集成**：通过 REST API 或兼容 SDK 即可便捷接入。

## 合规与安全

我们高度重视您的数据安全。Verifik 采用高级加密（HTTPS / TLS 1.3）和严格的隐私管理标准以确保机密性。
我们的服务全天候 24/7 监控可用性，并提供基于角色的访问控制以保障团队访问安全。

## 关于 Verifik

Verifik 是拉丁美洲领先的身份核验、合规与反欺诈平台。
我们的 API 自动化 KYC、KYB、AML 与生物识别验证流程，将企业与哥伦比亚、墨西哥、秘鲁、智利、乌拉圭、阿根廷、巴西、哥斯达黎加及更多地区的官方数据源相连。
