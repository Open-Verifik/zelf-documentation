---
id: colombia-registraduria-matrimonio
title: 哥伦比亚 — 婚姻民事登记（SCCRC）
description: 通过 GET /v2/co/registraduria/matrimonio（Registraduría SCCRC），按证件、姓名或序列号搜索哥伦比亚婚姻民事登记记录。
slug: /identity/colombia-registraduria-matrimonio
keywords:
  - v2/co/registraduria/matrimonio
  - /v2/co/registraduria/matrimonio
  - v2 co registraduria matrimonio
  - api/co/registraduria/matrimonio
  - /api/co/registraduria/matrimonio
  - api.verifik.co/v2/co/registraduria/matrimonio
  - 'https://api.verifik.co/v2/co/registraduria/matrimonio'
  - GET v2/co/registraduria/matrimonio
  - GET /v2/co/registraduria/matrimonio
  - co/registraduria/matrimonio
  - registraduria/matrimonio
  - registro civil matrimonio
---

export const structuredData = {
	"@context": "https://schema.org",
	"@type": "TechArticle",
	headline: "哥伦比亚婚姻民事登记（SCCRC）| Verifik API",
	description:
		"按证件号、姓名或序列号查询 Registraduría SCCRC 婚姻民事登记簿。",
	articleSection: "API 文档",
	keywords: "哥伦比亚, Registraduría, SCCRC, matrimonio, 婚姻, 身份 API",
	about: {
		"@type": "Thing",
		name: "身份核验 API",
	},
};

<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData) }} />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 哥伦比亚 — 婚姻民事登记（SCCRC）

查询 **Registraduría Nacional del Estado Civil**（**SCCRC**）的 **婚姻民事登记簿**（*registro civil de matrimonio*）。

请提供 **一种** 搜索模式：

1. **`documentNumber` + `sexo`** — 证件搜索（单条记录）
2. **`primerNombre` + `primerApellido` + `sexo` + `fecha`** — 姓名搜索（可能返回 **`matches`**）
3. **`serial`** — 序列号搜索（单条记录）

## 此 API 返回的内容

- 单条记录 **或** **`matches`** 数组（姓名模式）
- **`documentNumber`**、**`documentType`**、姓名、**`sexo`**、**`serial`**
- 可用时的 **`oficina`** / **`expeditionPlace`**、**`fecha`**
- **`recordType`** — `REGISTRO CIVIL DE MATRIMONIO`
- **已签名**的 Verifik 响应包装

## API 参考

### 端点

```
GET https://api.verifik.co/v2/co/registraduria/matrimonio
```

用于 **婚姻登记** 查询。同一集成也支持带 JSON 正文的 **`POST`**。**`GET`** 使用如下所示的**查询参数**。

### 请求头（Headers）

| Name | Value |
| --- | --- |
| Accept | `application/json` |
| Authorization | `Bearer <token>` |

### 参数（Parameters）

| name | type | required | description | Examples |
| --- | --- | --- | --- | --- |
| `documentNumber` | string | conditional | NUIP/CC；需要 **`sexo`** | `10000001` |
| `sexo` | string | conditional | 证件或姓名搜索时必填（`MASCULINO` / `FEMENINO`） | `MASCULINO` |
| `serial` | string | conditional | 婚姻登记序列号 | `0042012345` |
| `primerNombre` | string | conditional | 第一名（姓名模式） | `JUAN` |
| `primerApellido` | string | conditional | 第一姓（姓名模式） | `PEREZ` |
| `fecha` | string | conditional | 结婚日期 **`DD/MM/YYYY`**（姓名模式） | `20/06/2015` |
| `segundoNombre` | string | no | 第二名 | `CARLOS` |
| `segundoApellido` | string | no | 第二姓 | `GOMEZ` |

### 请求（Request）

<Tabs>
  <TabItem value="javascript" label="JavaScript">

```javascript
import axios from "axios";

const { data } = await axios.get("https://api.verifik.co/v2/co/registraduria/matrimonio", {
	params: {
		documentNumber: "10000001",
		sexo: "MASCULINO",
	},
	headers: {
		Accept: "application/json",
		Authorization: `Bearer ${process.env.VERIFIK_TOKEN}`,
	},
});
console.log(data);
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import os, requests

url = "https://api.verifik.co/v2/co/registraduria/matrimonio"
headers = {"Accept": "application/json", "Authorization": f"Bearer {os.getenv('VERIFIK_TOKEN')}"}
params = {"documentNumber": "10000001", "sexo": "MASCULINO"}
r = requests.get(url, headers=headers, params=params)
print(r.json())
```

  </TabItem>
</Tabs>

### 响应（Response）

<Tabs>
  <TabItem value="200" label="200">

```json
{
	"data": {
		"documentType": "CC",
		"documentNumber": "10000001",
		"firstName": "JUAN CARLOS",
		"lastName": "PEREZ GOMEZ",
		"fullName": "JUAN CARLOS PEREZ GOMEZ",
		"arrayName": ["JUAN", "CARLOS", "PEREZ", "GOMEZ"],
		"sexo": "MASCULINO",
		"serial": "0042012345",
		"oficina": "NOTARIA UNICA - BOGOTA D.C.",
		"fecha": "20/06/2015",
		"recordType": "REGISTRO CIVIL DE MATRIMONIO"
	},
	"signature": {
		"dateTime": "July 20, 2026 12:00 PM",
		"message": "Certified by Verifik.co"
	}
}
```

  </TabItem>
  <TabItem value="404" label="404">

```json
{
	"code": "NotFound",
	"message": "Record not found."
}
```

  </TabItem>
  <TabItem value="409" label="409">

```json
{
	"code": "MissingParameter",
	"message": "Provide serial, or documentNumber+sexo, or name fields with sexo and fecha"
}
```

  </TabItem>
</Tabs>

## 功能

- 三种搜索模式：**证件**、**姓名** 或 **序列号**
- 姓名模式可能返回 **`matches`**；证件/序列号返回单个对象
- **GET** 与 **POST** 共用同一处理程序

## 使用场景

- 为 KYC / 合规确认 **婚姻登记** 命中
- 解析与已知证件关联的婚姻序列号
- 仅有传记数据时的姓名筛查

### 说明（Notes）

- 请**恰好提供一种**搜索模式（不要将序列号与证件/姓名字段混用）。
- **沙箱：** 证件号 **`10000001`–`10000010`**；姓名模式遵循与[按姓名查询](/verifik-zh/identity-validation/colombia/colombian-citizen-by-name)相同的沙箱档案。
- 来源暂时不可用时可能表现为 **409**（`Endpoint_out_of_service`）。
- 相关：[按证件查询公民](/verifik-zh/identity-validation/colombia/colombian-citizen)、[按姓名查询公民](/verifik-zh/identity-validation/colombia/colombian-citizen-by-name)、[按序列号查询民事登记](/verifik-zh/identity/colombia-registraduria-serial)、[Registraduría 证明](/verifik-zh/identity/colombia-registraduria-certificate)。
