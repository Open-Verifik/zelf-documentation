---
id: actualizar-una-validacion-de-documento
title: Actualizar una Validación de Documento
description: Actualizar un registro de validación de documento existente
slug: /recursos/actualizar-una-validacion-de-documento
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Actualizar una Validación de Documento

## Endpoint

```
PUT https://api.verifik.co/v2/document-validations/{id}
```

Método para actualizar una Validación de Documento existente. Para que el servicio funcione, se requiere el parámetro `_id`, que se genera cuando una Validación de Documento se crea correctamente.

:::warning
La funcionalidad de actualización de Validación de Documento actualmente no está implementada en el sistema. Este endpoint devolverá un error "method_not_set" si se llama.
:::

### Headers

| Name          | Value                        |
| ------------- | ---------------------------- |
| Content-Type  | `application/json`           |
| Authorization | `Bearer {YOUR_ACCESS_TOKEN}` |

### Parámetros de Ruta

| Name | Type   | Required | Description                                                      |
| ---- | ------ | -------- | ---------------------------------------------------------------- |
| `id` | string | **Sí**   | El identificador único de la validación de documento a actualizar.      |

### Parámetros del Cuerpo

| Name           | Type   | Required | Description                                    |
| -------------- | ------ | -------- | ---------------------------------------------- |
| `status`       | string | No       | Actualizar el estado de validación                   |
| `documentData` | object | No       | Actualizar campos de datos del documento                    |
| `riskScore`    | number | No       | Actualizar puntuación de evaluación de riesgo                   |

### Solicitud

<Tabs>
  <TabItem value="javascript" label="JavaScript">

```javascript
import axios from 'axios';

const options = {
  method: 'PUT',
  url: 'https://api.verifik.co/v2/document-validations/document_validation_123456789',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer <tu_token>'
  },
  data: {
    status: 'ACTIVE',
    documentData: {
      firstName: 'John',
      lastName: 'Doe'
    },
    riskScore: 0.05
  }
};

try {
  const { data } = await axios.request(options);
  console.log(data);
} catch (error) {
  console.error(error);
}
```

  </TabItem>
  <TabItem value="python" label="Python">

```python
import http.client
import json

conn = http.client.HTTPSConnection("api.verifik.co")

payload = json.dumps({
  "status": "ACTIVE",
  "documentData": {
    "firstName": "John",
    "lastName": "Doe"
  },
  "riskScore": 0.05
})

headers = {
  'Content-Type': 'application/json',
  'Authorization': 'Bearer <tu_token>'
}

conn.request("PUT", "/v2/document-validations/document_validation_123456789", payload, headers)

res = conn.getresponse()
data = res.read()

print(data.decode("utf-8"))
```

  </TabItem>
  <TabItem value="php" label="PHP">

```php
<?php

$client = new \GuzzleHttp\Client();

$response = $client->request('PUT', 'https://api.verifik.co/v2/document-validations/document_validation_123456789', [
  'headers' => [
    'Content-Type' => 'application/json',
    'Authorization' => 'Bearer <tu_token>',
  ],
  'json' => [
    'status' => 'ACTIVE',
    'documentData' => [
      'firstName' => 'John',
      'lastName' => 'Doe'
    ],
    'riskScore' => 0.05
  ]
]);

echo $response->getBody();
```

  </TabItem>
  <TabItem value="swift" label="Swift">

```swift
import Foundation

let headers = [
  "Content-Type": "application/json",
  "Authorization": "Bearer <tu_token>"
]

let parameters = [
  "status": "ACTIVE",
  "documentData": [
    "firstName": "John",
    "lastName": "Doe"
  ],
  "riskScore": 0.05
] as [String : Any]

let postData = try JSONSerialization.data(withJSONObject: parameters, options: [])

let request = NSMutableURLRequest(url: NSURL(string: "https://api.verifik.co/v2/document-validations/document_validation_123456789")! as URL,
                                        cachePolicy: .useProtocolCachePolicy,
                                    timeoutInterval: 10.0)
request.httpMethod = "PUT"
request.allHTTPHeaderFields = headers
request.httpBody = postData as Data

let session = URLSession.shared
let dataTask = session.dataTask(with: request as URLRequest, completionHandler: { (data, response, error) -> Void in
  if (error != nil) {
    print(error as Any)
  } else {
    let httpResponse = response as? HTTPURLResponse
    print(httpResponse)
  }
})

dataTask.resume()
```

  </TabItem>
</Tabs>

### Respuesta

<Tabs>
  <TabItem value="405" label="405">

```json
{
  "error": "Método no permitido",
  "message": "method_not_set"
}
```

  </TabItem>
</Tabs>

### Notas Importantes

* **Actualización No Disponible**: La funcionalidad de actualización de Validación de Documento actualmente no está implementada en el sistema.
* **Métodos Alternativos**: Para modificar datos de validación de documentos, puedes necesitar:
  * Crear un nuevo registro de Validación de Documento
  * Usar el endpoint de validación de nombre (`PUT /document-validations/{id}/validate`) para actualizaciones de validación específicas
  * Contactar soporte para actualizaciones manuales si es necesario

### Características

- **Endpoint Documentado**: Documentación completa del endpoint de actualización
- **Parámetros de Actualización**: Estado, datos del documento y puntuación de riesgo
- **Advertencia de Implementación**: Nota clara sobre la falta de implementación actual
- **Métodos Alternativos**: Sugerencias para alternativas de actualización
- **Múltiples Lenguajes**: Soporte para JavaScript, Python, PHP y Swift
- **Manejo de Errores**: Respuesta de error apropiada para método no implementado
- **Documentación Completa**: Información detallada sobre limitaciones y alternativas
